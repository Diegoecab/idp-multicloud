apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: mysql-oci
  labels:
    db.platform.example.org/provider: oci
    db.platform.example.org/class: mysql
spec:
  compositeTypeRef:
    apiVersion: db.platform.example.org/v1alpha1
    kind: MySQLInstance
  resources:
    - name: managed-mysql
      # Replace this placeholder with your provider-specific OCI MySQL resource.
      base:
        apiVersion: kubernetes.crossplane.io/v1alpha2
        kind: Object
        spec:
          forProvider:
            manifest:
              apiVersion: v1
              kind: ConfigMap
              metadata:
                name: placeholder-oci-mysql
              data:
                note: "Replace with provider-oci MySQL resource"
      patches:
        - fromFieldPath: spec.parameters.region
          toFieldPath: spec.forProvider.manifest.data.region
        - fromFieldPath: spec.parameters.provider
          toFieldPath: spec.forProvider.manifest.data.provider
